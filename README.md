# liz-planner

Personal planning page — live at the GitHub Pages URL for this repo.

- Edit `index.html` in the browser via GitHub's pencil icon; Pages redeploys in ~60s.
- Checkmarks persist via `localStorage` (per-browser, per-device).
